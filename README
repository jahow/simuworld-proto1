# Installation

You will need npm and bower installed for this to work.

1. clone repo
2. install typescript globally with ```npm install typescript -g
2. open an invite in the root directory
3. type ```npm install
3. type ```bower install